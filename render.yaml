databases:
  - name: app-db
    databaseName: appdb
    user: appuser

services:
  - type: web
    name: nodejs-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: app-db
          property: connectionString
      - key: POSTGRES_USER
        value: appuser
      - key: POSTGRES_PASSWORD
        generateValue: true  
      - key: POSTGRES_DB
        value: appdb